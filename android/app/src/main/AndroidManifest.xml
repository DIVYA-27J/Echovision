<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ================================================================
         Echo Vision — AndroidManifest.xml
         All permissions required by the app as documented in Part 6
         of the Echo Vision Full Project Guide.
         ================================================================ -->

    <!-- MANDATORY: Required for live object detection -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- MANDATORY: Required for "Help me" voice command detection -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- OPTIONAL: Included in emergency SMS for GPS coordinates -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- OPTIONAL: Send emergency SMS to contact -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- OPTIONAL: Telephony features -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- INTERNET: Required only if using cloud APIs in future -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->

    <!-- Required hardware features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:label="Echo Vision"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flutter embedding metadata -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

</manifest>
